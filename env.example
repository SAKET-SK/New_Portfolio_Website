# Copy this file to .env for local use with `vercel dev`.
# Never commit your real .env file or API key.
#
# In production (Vercel/Netlify dashboard), set this as a project
# environment variable instead of committing a .env file.

OPENAI_API_KEY=sk-your-key-here
